<!-- Main Navigation -->
  <nav class="navbar navbar-light navbar-expand-lg">
    <a class="navbar-brand" href="/"><img id="logo" src="Assets/images/logo.png" alt=""></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">

          <li class="nav-item">
            <% if (title == "Home") { %>
              <a class="nav-link active"  href="/home"><i class="fas fa-house-user"></i> Home</a>
            <% } else { %>
              <a class="nav-link"  href="/home"><i class="fas fa-house-user"></i> Home</a>
            <% } %>
          </li>

          <li class="nav-item">
            <% if (title == "Survey") { %>
              <a class="nav-link active"  href="/add"><i class="fas fa-plus-circle"></i> Create Survey</a>
            <% } else { %>
              <a class="nav-link"  href="/add"><i class="fas fa-plus-circle"></i> Create Survey</a>
            <% } %>
          </li>

          <li class="nav-item">
            <% if (title == "Sign Up") { %>
              <a class="nav-link active"  href="/signup"> <i class="fas fa-user-plus"></i> Sign Up</a>
            <% } else { %>
              <a class="nav-link"  href="/signup"><i class="fas fa-user-plus"></i> Sign Up</a>
            <% } %>
          </li>

        <% if(typeof username == 'string' && username){ %>
          <li class="nav-item">
              <a class="nav-link active"  href="/logout"><i class="fas fa-sign-out-alt"></i> Sign Out</a>
          </li>
         <% } else{ %>
          <li class="nav-item">
            <% if (title == "Sign In") { %>
              <a class="nav-link active"  href="/signin"><i class="fas fa-sign-in-alt"></i> Sign In</a>
            <% } else { %>
              <a class="nav-link"  href="/signin"><i class="fas fa-sign-in-alt"></i> Sign In</a>
            <% } %>
          </li>
          <% } %>


    </div>
  </nav>
